from django.apps import AppConfig


class WebservingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.webserving'
    verbose_name = 'WebServing — Local Inventory Router'
